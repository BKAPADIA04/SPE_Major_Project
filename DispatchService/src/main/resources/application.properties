server.port=8082
spring.application.name=dispatch-service
management.endpoints.web.exposure.include=health,info

# URL of the Ambulance Location Service
ambulance.service.base-url=${AMBULANCE_SERVICE_BASE_URL}
spring.docker.compose.enabled=false

server.servlet.context-path=/dispatch
