apiVersion: v1
kind: Service
metadata:
  name: ambulance-service
  namespace: spe-system
spec:
  type: NodePort
  selector:
    app: ambulance-service
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30083
