apiVersion: v1
kind: Service
metadata:
  name: dispatch-service
  namespace: spe-system
spec:
  type: NodePort
  selector:
    app: dispatch-service
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
